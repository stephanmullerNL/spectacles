import { VotePowerPipe } from './vote-power.pipe';

describe('VotePowerPipe', () => {
  it('create an instance', () => {
    const pipe = new VotePowerPipe();
    expect(pipe).toBeTruthy();
  });
});
