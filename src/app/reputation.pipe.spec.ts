import { ReputationPipe } from './reputation.pipe';

describe('ReputationPipe', () => {
  it('create an instance', () => {
    const pipe = new ReputationPipe();
    expect(pipe).toBeTruthy();
  });
});
